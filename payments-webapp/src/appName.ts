// The brand, as the wireframes' navbar spells it — imported wherever the app
// name is needed (main.tsx tab title is set in index.html; App.tsx's splash
// and NoAccess copy read this) so it is declared exactly once.
export const APP_NAME = "Merchant Payments";
